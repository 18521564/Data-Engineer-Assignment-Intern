Question1:
Assume that we have a table storing scores of athletes in a competition:
Performance(AthleteId, Gender, Country, Score).
Write an SQL to find the second highest score of athletes. (15 points)

Answer:
select MAX(Score)
from Performance
where Score < (select MAX(Score) from Performance);
